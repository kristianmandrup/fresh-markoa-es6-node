import AppViewPathsResolver from '../../../../../src/app/loader/resolver/app-view-paths-resolver';

describe('AppViewPathsResolver', function() {
  it('exists', () => {
    expect(AppViewPathsResolver).to.not.be.undefined;
  });
});