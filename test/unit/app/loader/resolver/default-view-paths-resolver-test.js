import DefaultViewPathsResolver from '../../../../../src/app/loader/resolver/default-view-paths-resolver';

describe('DefaultViewPathsResolver', function() {
  it('exists', () => {
    expect(DefaultViewPathsResolver).to.not.be.undefined;
  });
});