'use strict';

import DataConfigurator from './data-configurator';

export default class Content extends DataConfigurator {
  get data() {
    return {
      index: {
      },
      prematch: {
      },
      live: {
      }
    };
  }
}
